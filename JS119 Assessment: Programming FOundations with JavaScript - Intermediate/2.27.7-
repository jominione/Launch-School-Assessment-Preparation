// What will the following code log to the console and why? Don't run it until 
// you have tried to answer.

function capitalize(word) {
  return word[0].toUpperCase() + word.slice(1);
}

function exclaim(word) {
  return (word += "!!!");
}

let word = "hello";
let capitalizedWord = capitalize(word);
let exclaimedWord = exclaim(capitalizedWord);

console.log(word);
console.log(capitalizedWord);
console.log(exclaimedWord);

// Solution: Will output `hello`, `Hello`  and `Hello!!!`.

// On line 4 to 6 a function declaration `captialize` is defined with parameter
// `word`.

// The function body on line 5 returns `word` capitalised.

// Strings are immutable so any string value passed as an argument are unaffected
// by the function so the function behaves like pass-by-value.

// On line 8 to 10 a function declaration `exclaim` is defined with parameter
// `word` which has function scope.

// The function body on line 9 returns `word` concatenated with `'!!!'`.

// Strings are immutable so any string value passed as an argument are unaffected
// by the function so the function behaves like pass-by-value.

// On line 12 a global variable `word` is declared and initialised with string
// value `"hello".

// On line 13 a global variable `captializedWord` is declared and initialised
// with the return value of invoking `capitalize` function with the value of
// global variable `word` passed as an argument.

// On line 14 a global variable `exclaimedWord` is declared and initialised
// with the return value of invoking `exclaim` function with the value of
// global variable `capitalizedWord` passed as an argument.

// On line 16 `console.log` is invoked with the value of global variable
// `word` passed as an argument. It outputs `hello`.

// On line 17 `console.log` is invoked with the value of global variable
// `capitalizedWord` passed as an argument. It outputs the return value
// of invoking `capitalizedWord` which is `Hello`.

// On line 18 `console.log` is invoked with the value of global variable
// `exclaimedWord` passed as an argument. It outputs the return value
// of invoking `exclaim` function which is `Hello!!!`.