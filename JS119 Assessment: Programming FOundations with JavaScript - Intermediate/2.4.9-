// Identify all of the variables named on each line of the following code.
// You may assume that question is the name of a built-in function in 
// JavaScript (it is not, so this code won't work as written).

// Example:

function multiply(left, right) {
  let product = left * right;
  return product;
}

function getNumber(prompt) {
  return parseFloat(question(prompt));
}

let left = getNumber('Enter the first number: ');
let right = getNumber('Enter the second number: ');
console.log(`${left} * ${right} = ${multiply(left, right)}`);

// Solution: 

// On line 7 the variables are `multiply`, `left`, `right`.
// On line 8 the variables are `product`, `left`, `right`.
// On line 9 the variable is `product`.

// On line 12 the variables are `getNumber`, `prompt`.
// On line 13 the variables are global method `parseFloat`, `question`, `prompt`.

// On line 16 the variables are global variables `left`, function name `getNumber`.
// On line 17 the variables are global variables `right`, function name `getNumber`.
// On line 18 the variables are`console`, `left`, `right`, `multiply`