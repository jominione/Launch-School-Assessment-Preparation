// Identify all of the variables, object property names, primitive values,
// and objects shown in the following code (assume the code has not been 
// executed). Don't panic if you miss a few items - this exercise is more
// challenging than it looks.

function hello(greeting, name) {
  return greeting + ' ' + name;
}

function xyzzy() {
  return { a: 1, b: 2, c: [3, 4, 5], d: {} };
}

const howdy = hello('Hi', 'Grace');
let foo = xyzzy();

// Solution: 

// Variables:
// - function name: `hello`, `xyzzy`;
// - parameters: `greeting`, `name`;
// - global variables: `howdy`, `foo`;
// - 

// Object Property Names:
// - Object keys of object and indexes of nested array on line 11: `'a'`, `'b'`, `'c'`, `'d'`, `0`, `1`, `2`;

// Primitive Values:
// - Line 7, `' '`;
// - values of object and nested array on line 11: `1`, `2`, `3`, `4`, `5`;
// - arguments of `howdy` on line 14: `'Hi'`, `'Grace'`;
// - Object keys of object and indexes of nested array on line 11: `'a'`, `'b'`, `'c'`, `'d'`, `0`, `1`, `2`;
// - return value of invoking `hello` on line 14: `'Hi Grace`;

// Objects:
// - function declarations: `hello`, `zyzzy`;
// - object on line 11: `{ a: 1, b: 2, c: [3, 4, 5], d: {} }`, `[3, 4, 5]`, `{}`;
// - 