// Will the code below raise an error?

let numbers = [1, 2, 3];
numbers[6] = 5;

// Solution: 

// Bonus:

let numbers = [1, 2, 3];
numbers[6] = 5;
numbers[4];  // what will this line return?

// Solution to Bonus: 