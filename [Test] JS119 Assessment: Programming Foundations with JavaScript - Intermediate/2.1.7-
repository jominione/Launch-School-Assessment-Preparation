// Will an error occur if you try to access an array element with an index
// that is greater than or equal to the length of the array? For example:

let foo = ['a', 'b', 'c'];
console.log(foo.length);  // => 3
console.log(foo[3]);      // will this result in an error?

// Solution: Will output `3` and `undefined`.

// When you access an array by referening an index that does not exist it will
// return `undefined`.